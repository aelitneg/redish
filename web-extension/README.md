# Unreadly Web Extension

The browser extension for the Unreadly app.

## Setup

1. Install the [web-ext](https://extensionworkshop.com/documentation/develop/getting-started-with-web-ext/) tool
1. From the `./web-extension` directory, run the tool
   ```sh
   web-ext run [--devtools]
   ```
